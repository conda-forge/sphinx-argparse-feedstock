{% set name = "sphinx-argparse" %}
{% set version = "0.2.4" %}
{% set hash_type = "sha256" %}
{% set hash_val = "23848ddd84544d0bf71f6fb4f36ed964aacc0428965150542e00cd3bde927363" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  {{ hash_type }}: {{ hash_val }}

build:
  noarch: python
  number: 0
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  host:
    - pip
    - python
    - setuptools

  run:
    - python
    - commonmark >=0.5.6,<0.8.1
    - sphinx

test:
  imports:
    - sphinxarg

about:
  home: https://github.com/ribozz/sphinx-argparse
  license: MIT
  summary: 'Sphinx extension that automatically documents argparse commands and options'

extra:
  recipe-maintainers:
    - pmlandwehr
    - croth1
    - dpryan79
